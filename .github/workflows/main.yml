on: [push]

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Scan the webapplication
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: main
      - name: ZAP Scan
        uses: zaproxy/action-full-scan@v0.5.1
        with:
          target: 'http://localhost:5187/bookstore?bookid=65&isloggedin=true'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
